# ============================================
# 123MiniApps.online, Blog articles: DEVELOPER tools
# One dedicated 1000-1200 word article per developer tool.
# Consumed by build-blog.py via POST_MODULES.
# ============================================

POSTS = [
 {
 'slug': 'base64-encoding-explained-for-developers',
 'icon': '🔤',
 'nav_title': 'Base64 encoding',
 'headline': 'Base64 Encoding Explained: What It Is and When to Use It',
 'title': 'Base64 Encoding Explained: What It Is & When to Use It | 123MiniApps',
 'standfirst': 'What Base64 actually does, why it is not encryption, and the everyday jobs it solves for developers.',
 'description': 'What Base64 encoding is and when to use it. How it turns binary into safe text, why it is not encryption, and a free in-browser Base64 encoder and decoder.',
 'published': '2026-08-02',
 'keywords': ['base64 encode', 'base64 decode', 'what is base64 encoding', 'base64 encoder decoder', 'encode string base64', 'base64 online'],
 'related_tools': [('base64-encoder-decoder', 'Base64 Encoder / Decoder'), ('url-encoder-decoder', 'URL Encoder / Decoder'), ('jwt-decoder', 'JWT Decoder'), ('image-to-base64', 'Image to Base64')],
 'body': [
 ('p', 'Base64 is an encoding that turns arbitrary binary data into plain, printable text using a set of 64 safe characters. Developers meet it constantly, in data URIs, email attachments, JSON Web Tokens, API responses and config files, because it lets binary data travel safely through systems that were designed to carry text. The <a href="../tools/base64-encoder-decoder.html">Base64 Encoder / Decoder</a> converts either way in your browser, and this article explains what Base64 does, what it does not do, and when to reach for it.'),
 ('p', 'The single most important thing to understand up front is that Base64 is encoding, not encryption. It scrambles nothing and hides nothing, anyone can decode it instantly. Confusing the two is the source of real security mistakes, so it is worth being clear about from the start.'),
 ('h2', 'The problem Base64 solves'),
 ('p', 'Many systems are text-only. Email was designed to carry text; JSON and XML are text formats; URLs are text. Binary data, an image, an encryption key, a compiled file, contains byte values that these text systems can misinterpret or corrupt, because some byte values are treated as control characters rather than data. Base64 sidesteps the problem by re-expressing binary data using only letters, digits and a couple of symbols that every text system handles safely. The data survives the journey intact and is decoded back to its original bytes at the other end.'),
 ('h2', 'How the encoding works'),
 ('p', 'Base64 takes the input three bytes (24 bits) at a time and splits those 24 bits into four groups of six. Each six-bit group indexes into an alphabet of 64 characters, A-Z, a-z, 0-9, plus two symbols, producing four output characters for every three input bytes. When the input length is not a multiple of three, the output is padded with one or two <code>=</code> signs so the length always comes out even. That three-to-four ratio is why Base64 output is always about 33% larger than the original data.'),
 ('callout', 'Base64 is not encryption', 'Encoding is public and reversible by design; encryption requires a key and hides meaning. A Base64 string labelled as "encoded" credentials protects nothing, decode it and the secret is right there. To actually protect data, use encryption, and keep Base64 for transport only.'),
 ('h2', 'Where developers meet Base64'),
 ('p', 'Once you recognise it, Base64 is everywhere:'),
 ('ul', ['<strong>Data URIs</strong> that embed images or fonts directly in HTML and CSS.', '<strong>JSON Web Tokens</strong>, whose three parts are Base64url-encoded, decodable with any decoder.', '<strong>Basic authentication</strong> headers, which Base64-encode the username and password (which is exactly why Basic auth must run over HTTPS).', '<strong>Email attachments</strong>, encoded so binary files survive text-based mail transport.', '<strong>Binary fields in JSON APIs</strong>, since JSON has no native binary type.']),
 ('h2', 'Base64 vs Base64url'),
 ('p', 'There is a common variant worth knowing. Standard Base64 uses <code>+</code> and <code>/</code> as two of its characters, but those have special meaning in URLs. Base64url swaps them for <code>-</code> and <code>_</code> so the encoded text is safe to drop into a URL or filename without further escaping. It is the same idea with two characters changed; JSON Web Tokens use the url-safe variant. If a decode fails, a mismatch between the standard and url-safe alphabets is a frequent culprit.'),
 ('tool', 'base64-encoder-decoder', 'Base64 Encoder / Decoder', 'Encode text to Base64 or decode it back instantly, in your browser. Nothing you paste is uploaded, so even secrets stay on your device.'),
 ('h2', 'Encoding safely and privately'),
 ('p', 'Because Base64 is so often used with sensitive material, tokens, keys, credentials embedded in config, where you decode it matters. Pasting a token into a random online decoder means handing that token to a third party, and since Base64 hides nothing, they can read whatever it contained. A browser-based encoder and decoder does the transformation locally, so the data never leaves your machine. When you are inspecting a JWT or a credential, that difference is the line between a safe debugging step and a leak.'),
 ('p', 'For related transport encodings, a <a href="../tools/url-encoder-decoder.html">URL encoder/decoder</a> handles percent-encoding for URLs, and a <a href="../tools/jwt-decoder.html">JWT decoder</a> unpacks the Base64url parts of a token into readable JSON. Together they cover the encodings a developer runs into daily.'),
 ('h2', 'Common Base64 mistakes and how to avoid them'),
 ('p', 'A few recurring errors account for most Base64 headaches. The first is treating it as security: developers occasionally Base64-encode a password or API key and feel they have protected it, when in fact anyone can decode it in a second. Base64 obscures nothing; if a value must be secret, encrypt it and encode the ciphertext for transport, not the plaintext. The second is the alphabet mismatch already mentioned, feeding standard Base64 to a decoder expecting the url-safe variant, or vice versa, which produces garbage or an error until you match them.'),
 ('p', 'A third is forgetting about padding. Standard Base64 pads its output with <code>=</code> so the length is a multiple of four; some systems strip that padding, and a strict decoder may then reject the input. If a decode fails on otherwise valid-looking data, missing padding is a prime suspect. Finally, remember the size cost: because Base64 inflates data by about a third, encoding large payloads inside JSON or a URL can bloat requests noticeably. For anything sizeable, question whether the binary needs to travel inline at all, or whether a separate download would serve better. Keeping these four pitfalls in mind, it is not secret, match the alphabet, mind the padding, watch the size, turns Base64 from an occasional source of confusion into a predictable, reliable tool.'),
 ('p', 'Keep the <a href="../tools/base64-encoder-decoder.html">encoder and decoder</a> bookmarked for the moments Base64 shows up unannounced, an opaque field in an API response, a data URI you need to unpack, a token you want to inspect. Recognising it, decoding it locally, and remembering that it protects nothing will save you both time and the occasional security scare.'),
 ('p', 'In summary: Base64 is a reversible way to carry binary data through text-only systems, adding about a third to the size and providing exactly zero secrecy. Use it for transport and embedding, never for protection, watch for the url-safe variant, and decode sensitive values locally. Understand those points and one of the most common encodings in software stops being mysterious.'),
 ],
 },
 {
 'slug': 'url-encoding-percent-encoding-explained',
 'icon': '🔗',
 'nav_title': 'URL encoding',
 'headline': 'URL Encoding Explained: Why Spaces Become %20',
 'title': 'URL Encoding Explained: Percent-Encoding & %20 | 123MiniApps',
 'standfirst': 'What percent-encoding is, which characters need it, and why getting it wrong breaks links and query strings.',
 'description': 'URL encoding explained: why spaces become %20, which characters must be percent-encoded, and how to encode query parameters safely, with a free online tool.',
 'published': '2026-08-01',
 'keywords': ['url encode', 'url decode', 'percent encoding', 'what is %20', 'encode query string', 'url encoder decoder online'],
 'related_tools': [('url-encoder-decoder', 'URL Encoder / Decoder'), ('base64-encoder-decoder', 'Base64 Encoder / Decoder'), ('slug-generator', 'Slug Generator'), ('json-formatter', 'JSON Formatter')],
 'body': [
 ('p', 'If you have ever seen <code>%20</code> in a web address where a space should be, you have met URL encoding, also called percent-encoding. URLs are only allowed to contain a limited set of characters, so anything outside that set, spaces, accented letters, many symbols, has to be represented by a percent sign followed by two hexadecimal digits. The <a href="../tools/url-encoder-decoder.html">URL Encoder / Decoder</a> converts text to and from this form in your browser, and this article explains why it exists and how to use it correctly.'),
 ('p', 'Getting URL encoding right is not optional trivia: a single unencoded character in a query string can break a link, corrupt a search, or send the wrong data to a server. Understanding the rules prevents a whole class of frustrating bugs.'),
 ('h2', 'Why URLs cannot contain just anything'),
 ('p', 'The URL standard reserves a small alphabet of characters that are always safe, letters, digits, and a handful of symbols like hyphen, underscore, dot and tilde. Everything else falls into two groups: reserved characters that have a special structural meaning (like <code>/</code>, <code>?</code>, <code>#</code>, <code>&</code> and <code>=</code>), and unsafe characters (like spaces and many symbols) that can be mangled in transit. To include any of these as literal data rather than as structure, you must percent-encode them so the browser and server treat them as content, not syntax.'),
 ('h2', 'How percent-encoding works'),
 ('p', 'The mechanism is simple. Each character to be encoded is represented by its byte value in hexadecimal, prefixed with a percent sign. A space is byte 32, which is <code>20</code> in hex, so a space becomes <code>%20</code>. An ampersand is <code>%26</code>, a question mark <code>%3F</code>, a forward slash <code>%2F</code>. Characters outside basic ASCII, such as accented letters and emoji, are first expressed as their UTF-8 bytes and each byte is percent-encoded, which is why a single accented character can become several percent codes.'),
 ('callout', 'The plus-sign gotcha', 'In the query-string part of a URL, a space is sometimes encoded as + rather than %20, a legacy of old form submissions. The two are not interchangeable everywhere: %20 is always safe, while + means space only in query strings and a literal plus elsewhere. When in doubt, use %20.'),
 ('h2', 'Reserved characters and why they matter'),
 ('p', 'The characters that structure a URL are the ones most likely to cause bugs when they appear in data. Consider a search for the literal text "cats & dogs." If you drop that straight into a query parameter, the <code>&</code> is read as the separator between two parameters, so the server sees a parameter "cats " and a separate parameter "dogs", not your intended search. Percent-encoding the ampersand to <code>%26</code> tells the server it is part of the value, not a separator. The same logic applies to <code>=</code>, <code>?</code>, <code>#</code> and <code>/</code> when they appear inside data.'),
 ('h2', 'Encoding query parameters correctly'),
 ('p', 'The reliable rule when building URLs is to encode each parameter value individually, not the whole URL at once. Encoding the entire URL would wrongly encode the structural characters that are supposed to stay as syntax, the slashes, the question mark, the ampersands between parameters. Instead, take each value, encode it, and assemble the URL from the encoded pieces. This keeps the structure intact while making every value safe:'),
 ('ul', ['Encode the value, not the delimiters.', 'Assemble <code>?key=</code> + encoded value + <code>&key2=</code> + encoded value.', 'Never double-encode: encoding an already-encoded string turns <code>%20</code> into <code>%2520</code>.']),
 ('tool', 'url-encoder-decoder', 'URL Encoder / Decoder', 'Percent-encode text for safe use in URLs, or decode an encoded URL back to readable text, entirely in your browser.'),
 ('h2', 'Decoding to read what a URL really contains'),
 ('p', 'Decoding is just as useful as encoding. A long, percent-heavy URL from an analytics link, a redirect or an API call is unreadable until you decode it, at which point the parameters and their values become clear. This is invaluable for debugging: decode a URL to see exactly what was sent, spot a double-encoding problem, or understand why a link is not behaving. Because the decoder runs in your browser, you can safely decode URLs that contain tokens or personal data without sending them anywhere.'),
 ('h2', 'Encoding different parts of a URL'),
 ('p', 'One subtlety trips up even experienced developers: different parts of a URL have different rules about which characters are safe. A character that must be encoded in a query-string value might be perfectly fine in the path, and the space-as-plus convention applies only to the query component, not the path. This is why a single blanket "encode everything" approach is wrong, it can encode characters that were meant to stay as structure, or fail to encode ones that needed it in a particular component.'),
 ('p', 'The practical defence is to use the right encoding function for the right job. Programming languages typically offer separate functions for encoding a whole URI versus encoding a single component, and the component-level function is the one you want for individual parameter values, because it encodes the reserved characters that would otherwise break the query structure. Build URLs by encoding each value with the component function and then joining them with the literal structural characters, the question mark, ampersands and equals signs, that you deliberately leave unencoded. Following that discipline consistently eliminates the whole family of bugs where a stray ampersand or equals sign in user data silently corrupts a request, and it means your links keep working even when someone searches for something full of symbols.'),
 ('p', 'Whenever a link misbehaves or a search returns the wrong thing, suspect encoding first. Decode the URL to see what was really sent, check for a stray reserved character in a value, and watch for the tell-tale <code>%2520</code> that betrays double-encoding. Nine times out of ten, a URL bug is an encoding bug, and a quick decode reveals it instantly.'),
 ('p', 'URL encoding sits alongside other transport encodings a developer uses daily, <a href="../tools/base64-encoder-decoder.html">Base64</a> for binary data, and clean <a href="../tools/slug-generator.html">slugs</a> for human-readable URLs. Master the rule that you encode values rather than structure, never double-encode, and prefer <code>%20</code> for spaces, and the mysterious percent codes in web addresses become an ordinary, predictable part of how the web works.'),
 ],
 },
 {
 'slug': 'how-to-decode-a-jwt-safely',
 'icon': '🎫',
 'nav_title': 'Decode a JWT',
 'headline': 'How to Decode a JWT (and Why You Should Never Paste One Online)',
 'title': 'How to Decode a JWT Safely: Structure & Claims Explained | 123MiniApps',
 'standfirst': 'The three parts of a JSON Web Token, what the claims mean, and why the tool you use to decode one matters for security.',
 'description': 'How to decode a JWT and read its claims. The header, payload and signature explained, why a JWT is not encrypted, and a safe in-browser decoder.',
 'published': '2026-07-31',
 'keywords': ['decode jwt', 'jwt decoder', 'what is inside a jwt', 'json web token structure', 'read jwt claims', 'jwt decode online'],
 'related_tools': [('jwt-decoder', 'JWT Decoder'), ('base64-encoder-decoder', 'Base64 Encoder / Decoder'), ('hash-generator', 'Hash Generator'), ('encryption-tool', 'Encryption Tool')],
 'body': [
 ('p', 'A JSON Web Token, or JWT, is the compact string an application hands you after you log in, which your browser then sends back to prove who you are. It looks like three chunks of gibberish separated by dots, but it is not random, each part is Base64url-encoded JSON that you can decode and read. The <a href="../tools/jwt-decoder.html">JWT Decoder</a> unpacks one in your browser, and this article explains the structure, what the claims mean, and the crucial security reason to decode tokens locally rather than pasting them into a random website.'),
 ('p', 'The headline safety point: a JWT usually identifies you to a system, so a valid token is like a temporary key to your account. Where you decode it matters enormously.'),
 ('h2', 'The three parts of a JWT'),
 ('p', 'Every JWT has three sections separated by dots, and each is Base64url-encoded:'),
 ('ul', ['<strong>Header</strong>: JSON describing the token type and the signing algorithm used, such as HS256 or RS256.', '<strong>Payload</strong>: JSON containing the claims: the actual data about who you are and what you can do.', '<strong>Signature</strong>: a cryptographic signature over the header and payload, used to verify the token has not been tampered with.']),
 ('p', 'Because the first two parts are only encoded, not encrypted, anyone can decode and read them. The signature is what stops anyone from changing them without detection.'),
 ('h2', 'What the claims mean'),
 ('p', 'The payload contains claims, many of which are standardised. You will commonly see <code>sub</code> (the subject, usually a user ID), <code>iat</code> (issued-at time), <code>exp</code> (expiry time), <code>iss</code> (issuer) and <code>aud</code> (audience). Alongside these, an application adds its own custom claims, a username, roles, permissions. Decoding a token to read these is the fastest way to answer questions like "why does this user see an admin menu?" or "has this token already expired?" The <code>exp</code> claim, a Unix timestamp, is especially useful for debugging authentication problems.'),
 ('callout', 'A JWT is signed, not encrypted', 'The payload is readable by anyone who has the token, it is only Base64-encoded. This means you must never put secrets in a JWT payload. The signature guarantees the token has not been altered; it does not hide the contents.'),
 ('h2', 'Signed versus encrypted, and what the signature proves'),
 ('p', 'The signature is the security heart of a JWT. When a server issues a token, it signs the header and payload with a secret (for HMAC algorithms) or a private key (for RSA and elliptic-curve algorithms). When the token comes back, the server recomputes the signature and checks it matches. If someone edits the payload, say, changing their role from "user" to "admin", the signature no longer matches and the token is rejected. The signature therefore proves integrity and authenticity, but it does not provide confidentiality; for that you would need an encrypted token, which is a separate, less common construct.'),
 ('h2', 'Why you must not paste tokens into random sites'),
 ('p', 'Here is the security crux. A live JWT is a bearer token: whoever holds it can act as you until it expires. Many online JWT decoders send the token to their server to decode it, which means you have just handed a working key to your account to an unknown third party. Even if they are honest, the token may sit in their logs. If it has not yet expired, that is a real account-takeover risk. The only safe way to inspect a token you care about is to decode it entirely on your own device.'),
 ('tool', 'jwt-decoder', 'JWT Decoder', "Decode a JWT's header and payload and read every claim, entirely in your browser. The token is never sent anywhere, so live tokens stay safe."),
 ('h2', 'A practical decoding workflow'),
 ('p', 'When debugging authentication, decode the token to check three things first: is it expired (compare <code>exp</code> to the current time), who is it for (<code>sub</code>, <code>aud</code>), and what permissions does it carry (your custom claims). Because each part is just Base64url, you can even decode the pieces manually with a <a href="../tools/base64-encoder-decoder.html">Base64 decoder</a> if you want to see the mechanism, though a dedicated decoder is faster and handles the url-safe alphabet automatically.'),
 ('h2', 'Debugging authentication with a decoded token'),
 ('p', 'Decoding a JWT is one of the most efficient ways to diagnose an authentication problem, because the token records exactly what the server believes about the session. When a user reports being unexpectedly logged out, decode their token and check the <code>exp</code> claim against the current time, an expired token is the most common culprit, and the timestamps make it obvious. When a user cannot access something they should, decode the token and inspect the role or permission claims: the answer is often that the token was issued before their permissions changed and simply has not been refreshed yet.'),
 ('p', 'The claims also help when integrating with an identity provider. The <code>iss</code> and <code>aud</code> claims tell you which service issued the token and which service it is meant for, and a mismatch there explains many "invalid token" rejections. Because all of this information sits in the readable payload, decoding is usually faster than adding logging or stepping through code. The one rule that never bends: do this decoding locally. A token you are debugging is, by definition, a token that currently works, which makes it exactly the kind of credential you must never paste into a third-party site. A browser-based decoder gives you every claim you need while keeping the live token on your own machine.'),
 ('p', 'Make local decoding a habit rather than an exception. The moment you paste a working token into a website to "just quickly check it," you have potentially handed over the keys to an account. A browser-based decoder removes that temptation entirely, giving you the full payload in a second without the token ever leaving your machine, which is exactly how inspecting a live credential should always work.'),
 ('p', 'To recap: a JWT is three Base64url-encoded parts, a readable header and payload plus a signature that guarantees they have not been altered. It is signed, not secret, so never store confidential data in it, and never paste a live token into a website that decodes it server-side. Decode locally, read the claims, check the expiry, and the token stops being an opaque string and becomes a clear, debuggable record of a session.'),
 ],
 },
 {
 'slug': 'why-format-your-html',
 'icon': '📄',
 'nav_title': 'Format HTML',
 'headline': 'Why You Should Format Your HTML (and What a Formatter Actually Does)',
 'title': 'HTML Formatter: Why Clean, Indented Markup Matters | 123MiniApps',
 'standfirst': 'How proper indentation reveals structure, why minified HTML is unreadable, and how a formatter makes markup maintainable again.',
 'description': 'Why formatting your HTML matters and what an HTML formatter does. How indentation reveals nesting, fixing messy or minified markup, and a free in-browser tool.',
 'published': '2026-07-30',
 'keywords': ['html formatter', 'format html online', 'beautify html', 'indent html', 'clean up html', 'prettify html'],
 'related_tools': [('html-formatter', 'HTML Formatter'), ('css-minifier', 'CSS Minifier'), ('javascript-minifier', 'JavaScript Minifier'), ('markdown-preview', 'Markdown Preview')],
 'body': [
 ('p', 'HTML is a nested language, elements sit inside other elements, sometimes many levels deep, and that structure is only visible when the markup is properly indented. Formatting HTML means reindenting it so each level of nesting is clear, turning a wall of tags into a readable tree. The <a href="../tools/html-formatter.html">HTML Formatter</a> does this in your browser, and this article explains why well-formatted markup matters and what a formatter actually changes.'),
 ('p', "Whether you are debugging a layout, learning from a page's source, or cleaning up markup pasted from somewhere, readable HTML is the difference between understanding the structure at a glance and squinting at an undifferentiated blob."),
 ('h2', 'Why indentation reveals structure'),
 ('p', 'In HTML, meaning comes from nesting: a list item belongs to a list, a table cell to a row, a paragraph to a section. When each nested element is indented one level deeper than its parent, that hierarchy becomes visible, you can see at a glance which elements contain which, where a section opens and where it closes. Without indentation, the same markup is a flat run of tags where the relationships are invisible, and finding the closing tag that matches a given opening tag becomes a manual counting exercise.'),
 ('h2', 'The problem with minified and messy HTML'),
 ('p', 'HTML arrives messy for two main reasons. Minified HTML has had all its whitespace stripped to save bytes for faster loading, great for production, unreadable for humans. Copy-pasted or generated HTML often has erratic, inconsistent indentation that is arguably worse than none, because it implies a structure that does not match reality. In both cases a formatter re-derives the true structure from the tags themselves and re-indents consistently, so what you read matches how the browser actually parses it.'),
 ('callout', 'Format for editing, minify for shipping', 'Readable, indented HTML is for development, writing, reading and debugging. Minified HTML is for production, where smaller files load faster. The two are the same markup in different clothes; format when you work on it, minify when you ship it.'),
 ('h2', 'What a formatter actually does'),
 ('p', 'A good HTML formatter does more than add spaces. It parses the markup into its element tree and re-emits it with consistent indentation, puts block-level elements on their own lines, and normalises the spacing inside tags. Better formatters also help you spot problems: badly nested or unclosed tags become obvious once everything is indented, because the indentation will not line up the way you expect. In this way formatting doubles as a lightweight validation step, surfacing structural mistakes that were hidden in the mess.'),
 ('h2', 'When formatting HTML helps most'),
 ('p', 'Reformatting markup pays off in several everyday situations:'),
 ('ul', ['Debugging a layout, where you need to see the nesting to understand why something is positioned oddly.', 'Reading the source of a page to learn how it was built.', 'Cleaning up HTML exported from a rich-text editor or a page builder, which is often deeply messy.', 'Reviewing markup in a pull request, where consistent formatting makes changes easier to read.', 'Un-minifying production HTML you need to inspect or modify.']),
 ('tool', 'html-formatter', 'HTML Formatter', 'Reindent and clean up messy or minified HTML into a readable structure, entirely in your browser. Your markup is never uploaded.'),
 ('h2', 'Formatting as part of a clean workflow'),
 ('p', 'HTML rarely travels alone. The same document usually carries CSS and JavaScript, and the same format-for-editing, minify-for-shipping logic applies to all three. When you are done editing, a <a href="../tools/css-minifier.html">CSS minifier</a> and a <a href="../tools/javascript-minifier.html">JavaScript minifier</a> compress the styles and scripts for production, while the HTML formatter keeps your working copy readable. If you are writing content rather than markup, a <a href="../tools/markdown-preview.html">Markdown preview</a> lets you author in a simpler syntax and see the resulting HTML.'),
 ('p', 'Because the formatter runs entirely in your browser, you can safely reformat markup from internal tools, client sites or unreleased pages without any of it being uploaded. That matters when the HTML contains data, structure or comments you would not want on a third-party server.'),
 ('h2', 'Formatting and accessibility go together'),
 ('p', 'There is a quieter benefit to well-formatted HTML: it makes structural and accessibility problems visible. Much of accessibility depends on using the right elements in the right hierarchy, headings that descend in order, lists marked up as lists, form fields tied to their labels, landmarks that wrap the main regions of a page. When markup is a flat, unindented blob, it is nearly impossible to see whether that structure is sound. Reformat it, and the outline of the page emerges: you can see whether headings nest properly, whether a list is really a list, whether the document has a sensible skeleton.'),
 ('p', 'This matters because screen readers and search engines both rely on that same structure. A heading that looks big because of styling but is actually a plain <code>div</code> gives a screen-reader user nothing to navigate by, and formatting the HTML makes that mistake obvious in a way a rendered page does not. Reviewing readable markup is therefore a fast, cheap accessibility check on top of its everyday debugging value. Reformat a page you are unsure about, read its structure top to bottom, and you will often spot missing landmarks, skipped heading levels or non-semantic elements standing in for real ones, the exact issues that hurt both accessibility and search visibility.'),
 ('p', 'Make formatting a reflex whenever you open unfamiliar or generated markup. A few seconds of reindentation turns an opaque wall of tags into a legible tree you can reason about, debug and check for accessibility, and because it all happens locally, even private or internal markup stays on your own machine while you work.'),
 ('p', 'The habit costs nothing and pays off every time: whenever markup looks impenetrable, format it first and read the structure before you try to change anything. Clear structure is the foundation of everything else, layout, accessibility and search all depend on it, so making that structure visible is rarely wasted effort.'),
 ('p', "The core idea is simple: HTML's meaning lives in its nesting, and only proper indentation makes that nesting visible. Format your markup while you work on it, let the formatter surface structural mistakes, and minify only when you ship. Readable HTML is easier to understand, faster to debug, and far less likely to hide the small nesting errors that cause big layout problems."),
 ],
 },
 {
 'slug': 'css-minification-explained',
 'icon': '🎯',
 'nav_title': 'CSS minification',
 'headline': 'CSS Minification: How Removing Whitespace Speeds Up Your Site',
 'title': 'CSS Minification Explained: Smaller Files, Faster Sites | 123MiniApps',
 'standfirst': 'What minification removes, how much it saves, and where it fits alongside concatenation and compression.',
 'description': 'What CSS minification does and how it speeds up your site. What gets removed, how much it saves, and how it fits with gzip and concatenation, plus a free minifier.',
 'published': '2026-07-29',
 'keywords': ['css minifier', 'minify css online', 'compress css', 'reduce css file size', 'css minification', 'minify stylesheet'],
 'related_tools': [('css-minifier', 'CSS Minifier'), ('javascript-minifier', 'JavaScript Minifier'), ('html-formatter', 'HTML Formatter'), ('gradient-generator', 'Gradient Generator')],
 'body': [
 ('p', 'Minifying CSS means stripping everything a browser does not need, spaces, line breaks, comments, redundant characters, so the stylesheet downloads faster while behaving identically. The browser does not care whether your CSS is neatly indented or crushed onto one line; it parses both the same way. But your visitors care, because a smaller file arrives sooner and the page renders faster. The <a href="../tools/css-minifier.html">CSS Minifier</a> does this in your browser, and this article explains what it removes, how much it saves, and where it fits in a performance strategy.'),
 ('p', 'Minification is one of the highest-value, lowest-risk optimisations available: it makes files smaller with no change in behaviour, and it is completely reversible for editing.'),
 ('h2', 'What minification removes'),
 ('p', 'A CSS minifier strips out everything that exists purely for human readability:'),
 ('ul', ['Whitespace, the indentation, spaces and line breaks that make CSS readable but add bytes.', 'Comments, helpful notes for developers that mean nothing to the browser.', 'The last semicolon in each rule block, which is optional.', 'Unnecessary units and leading zeros, <code>0px</code> becomes <code>0</code>, <code>0.5em</code> becomes <code>.5em</code>.', 'Redundant characters, collapsing the file to the smallest form the browser still understands.']),
 ('p', 'None of this changes what the CSS does. The rules, selectors and values are all preserved exactly; only the human-friendly padding is removed.'),
 ('h2', 'How much it actually saves'),
 ('p', 'Typical CSS shrinks by around 20-40% through minification alone, and more if the source has lots of comments and generous formatting. On a large stylesheet that can be tens of kilobytes saved on every single page load. The saving compounds because CSS is render-blocking, the browser must download and parse it before it can paint the page, so a smaller stylesheet does not just save bandwidth, it makes the page start rendering sooner. That is why minification is a standard step in every production build.'),
 ('callout', 'Minify the shipped file, keep the readable source', 'Never edit minified CSS by hand, it is unreadable. Keep your formatted, commented source as the version you work on, and minify a copy for production. Your build process should regenerate the minified file from the source, not the other way around.'),
 ('h2', 'Minification, concatenation and compression'),
 ('p', 'Minification is one of three complementary techniques, and understanding how they stack helps you get the most from each. Concatenation combines multiple CSS files into one, reducing the number of separate requests the browser must make. Minification shrinks the content of that file. Gzip or Brotli compression, applied by the web server, then compresses the file further for transmission. The three work together: concatenate to cut requests, minify to remove waste, and let the server compress what remains. Each addresses a different part of the delivery cost.'),
 ('h2', 'Where minification fits in your workflow'),
 ('p', 'The practical setup is to treat minification as a build step, not a manual chore. You write and maintain readable, commented CSS; a tool minifies it as part of publishing. During development you use the readable version so you can debug with meaningful selectors and comments; in production you serve the minified one. If you ever need to inspect a minified stylesheet, your own or someone else\'s, an <a href="../tools/html-formatter.html">HTML formatter</a>-style beautifier can re-expand it into readable form, since minification is fully reversible in terms of behaviour even though the comments are gone for good.'),
 ('tool', 'css-minifier', 'CSS Minifier', 'Strip whitespace and comments to shrink your CSS by up to 40%, entirely in your browser. Your stylesheet is never uploaded.'),
 ('h2', 'The same idea for JavaScript'),
 ('p', 'JavaScript benefits from the same treatment, and often more dramatically, because a <a href="../tools/javascript-minifier.html">JavaScript minifier</a> can also shorten internal variable names on top of removing whitespace. The principle is identical: ship the smallest file that behaves the same, and keep the readable source for development. Minifying both your CSS and JavaScript is one of the simplest ways to make a site measurably faster.'),
 ('h2', 'Why you should never lose the readable source'),
 ('p', 'The most important discipline around minification is keeping your original, readable stylesheet as the true source and treating the minified file as a disposable, regenerated output. Minified CSS is not meant to be edited: with all whitespace gone and comments stripped, finding and changing a rule by hand is slow and error-prone, and any edit risks introducing a hard-to-spot mistake. The correct model is that you always work on the formatted source, and a tool produces the minified version whenever you publish.'),
 ('p', 'This is why minification should ideally be automated as part of a build or deployment step rather than done manually and pasted around. An automated step guarantees the minified file always matches the current source, so the two never drift apart, a common bug when someone edits the minified file directly in a hurry and forgets to update the source, or vice versa. If you do only occasional minification by hand, adopt a simple rule: change the source, then regenerate the minified copy, never the reverse. Keeping that direction of flow one-way, source to minified, never back, is what keeps a stylesheet maintainable over months and years, letting you enjoy the performance benefit of small files without sacrificing the readability you need to actually work on them.'),
 ('p', 'Treat minification as free, automatic performance: set it up once as part of publishing, keep your readable source, and enjoy smaller, faster-loading stylesheets on every page without ever thinking about it again. It is one of the rare optimisations with real upside and essentially no downside.'),
 ('p', 'The summary is simple: readable source for people, minified output for browsers, generated automatically so the two never diverge. Adopt that one-way flow and minification quietly delivers smaller files and faster pages for the entire life of the project, with no ongoing effort and no readability lost.'),
 ('p', 'In short, CSS minification removes the bytes that exist only for human eyes, cutting file size by a third or so with zero change in behaviour and a real improvement in load time. Keep your readable source, minify a copy for production, and combine it with concatenation and server compression. It is free performance, smaller files, faster pages, happier visitors, for almost no effort.'),
 ],
 },
 {
 'slug': 'javascript-minification-and-performance',
 'icon': '⚡',
 'nav_title': 'JavaScript minification',
 'headline': 'JavaScript Minification: Smaller Bundles, Faster Load Times',
 'title': 'JavaScript Minification: Shrink Bundles for Faster Sites | 123MiniApps',
 'standfirst': 'How minifying JavaScript differs from minifying CSS, why it saves more, and how it fits with modern bundling.',
 'description': 'How JavaScript minification works and why it speeds up your site. Whitespace removal, name shortening, the difference from CSS, and a free in-browser minifier.',
 'published': '2026-07-28',
 'keywords': ['javascript minifier', 'minify js online', 'compress javascript', 'reduce js file size', 'js minification', 'minify javascript'],
 'related_tools': [('javascript-minifier', 'JavaScript Minifier'), ('css-minifier', 'CSS Minifier'), ('html-formatter', 'HTML Formatter'), ('json-formatter', 'JSON Formatter')],
 'body': [
 ('p', 'Minifying JavaScript means shrinking a script to the smallest form that still runs identically, so it downloads and parses faster. Like CSS minification it removes whitespace and comments, but JavaScript minifiers go further, they can also shorten internal variable and function names, which is why the savings are often larger. The <a href="../tools/javascript-minifier.html">JavaScript Minifier</a> does this in your browser, and this article explains how it works, why it matters more for JavaScript than for other assets, and where it fits alongside modern build tools.'),
 ('p', 'JavaScript is frequently the heaviest and most performance-critical asset a page loads, because the browser must not only download it but parse and execute it. Making it smaller helps on every one of those fronts.'),
 ('h2', 'What a JavaScript minifier does'),
 ('p', 'Minification of JavaScript happens in layers, each removing more:'),
 ('ul', ['<strong>Whitespace and comments</strong> are stripped, exactly as with CSS.', '<strong>Local variable and function names</strong> are shortened, a descriptive <code>calculateTotalPrice</code> inside a function might become <code>a</code>, since the name is irrelevant to how the code runs.', '<strong>Redundant syntax</strong> is simplified where it does not change behaviour.', '<strong>Dead code</strong> may be removed by more advanced tools, a process called tree-shaking.']),
 ('p', 'The name-shortening step is what sets JavaScript apart. Because internal names have no effect on behaviour, replacing long descriptive names with single letters can save a surprising amount on a large codebase, on top of the whitespace savings.'),
 ('h2', 'Why the savings matter more for JavaScript'),
 ('p', 'A byte of JavaScript costs more than a byte of most other assets, because the browser does more with it. An image is downloaded and displayed; a script is downloaded, parsed, compiled and executed, and all of that happens on the main thread that also handles user interaction. Large scripts can therefore make a page feel sluggish even after it appears to have loaded. Minification reduces the download and, by shrinking the file, also reduces parse time. On mobile devices with slower processors and networks, this difference is especially noticeable.'),
 ('callout', 'Minified is not obfuscated, and not secure', 'Minification shortens names for size, which makes code hard to read, but it is not a security measure. Anyone can run minified code through a formatter to make it readable again. Never rely on minification to hide secrets or logic; treat all client-side code as public.'),
 ('h2', 'Minification versus bundling'),
 ('p', "In modern development, minification usually happens as one stage of a larger build. A bundler combines your many source files and their dependencies into one or a few files, resolving imports along the way; a minifier then shrinks the result. The two solve different problems, bundling reduces the number of requests and manages dependencies, minification reduces size, and they run in sequence. For a small project or a single script, a standalone minifier is all you need; for a large application, minification is a step inside your bundler's production build."),
 ('h2', 'Keeping a readable source'),
 ('p', 'The golden rule mirrors CSS: never edit minified JavaScript by hand. The shortened names and stripped formatting make it practically unreadable, and a single mistake is very hard to spot. You keep your readable, well-named source as the thing you develop and debug, and minify a copy for production. When you need to inspect a minified script, perhaps to understand a third-party library or debug a production issue, run it through a formatter first to restore readable structure, remembering that the original variable names are gone.'),
 ('tool', 'javascript-minifier', 'JavaScript Minifier', 'Shrink JavaScript by removing whitespace and shortening names, entirely in your browser. Your code is never uploaded.'),
 ('h2', 'Part of a complete optimisation'),
 ('p', 'JavaScript minification is most effective as part of a full asset-optimisation pass. Minify your <a href="../tools/css-minifier.html">CSS</a> and JavaScript, keep your <a href="../tools/html-formatter.html">HTML</a> readable while you work and minified when you ship, and let your server apply gzip or Brotli compression on top. Together these routinely cut total page weight dramatically.'),
 ('h2', 'Source maps: debugging minified code'),
 ('p', "Minified JavaScript solves a performance problem but creates a debugging one: when an error occurs in production, the browser reports it in terms of the crushed, single-letter-variable code that no human can read, pointing at line one, column forty-thousand. The solution the industry settled on is the source map, a separate file that records how the minified code corresponds to your original source, so your browser's developer tools can translate a location in the minified file back to the exact line and variable name in the code you actually wrote."),
 ('p', 'With source maps in place, you get the best of both worlds: visitors download the small, fast minified file, while you debug against readable source as though no minification had happened. This is why modern build tools generate source maps automatically alongside the minified output. If you are minifying by hand for a small project you may not need them, but for any application where you will have to diagnose production errors, source maps turn minification from a debugging obstacle into a non-issue. The broader lesson is that minification is a production concern that should never compromise your ability to develop and debug, keep the readable source, generate source maps where you can, and treat the minified bundle purely as a delivery artefact.'),
 ('p', 'For any script that ships to real users, minify it, the download and parse savings are real, especially on mobile, but pair it with source maps and a preserved source so you never trade away your ability to debug. Small in production, readable in development: that is the balance every good build strikes, and it is entirely achievable.'),
 ('p', 'Bookmark a minifier for the quick jobs and lean on your build tool for the big ones, but always keep the readable source as your working copy. Ship small, develop readable, debug with source maps, get that balance right and heavy, script-driven pages feel noticeably faster to the people who use them.'),
 ('p', 'To summarise: JavaScript minification shrinks scripts by removing whitespace and comments and by shortening internal names, and because scripts are parsed and executed rather than merely displayed, those savings improve real-world responsiveness, especially on mobile. Keep your readable source, minify for production as a build step, and never treat minification as security. It is one of the most reliable ways to make a modern, script-heavy page load and respond faster.'),
 ],
 },
 {
 'slug': 'why-format-your-sql',
 'icon': '🗄️',
 'nav_title': 'Format SQL',
 'headline': 'Why Formatting Your SQL Makes Queries Easier to Read and Debug',
 'title': 'SQL Formatter: Readable Queries, Fewer Bugs | 123MiniApps',
 'standfirst': "How consistent formatting reveals a query's logic, why long queries become unreadable, and what a formatter standardises.",
 'description': 'Why formatting SQL matters and what a SQL formatter does. How indentation and keyword casing reveal query logic, making complex queries easier to read and debug.',
 'published': '2026-07-27',
 'keywords': ['sql formatter', 'format sql online', 'beautify sql', 'sql beautifier', 'prettify sql query', 'clean up sql'],
 'related_tools': [('sql-formatter', 'SQL Formatter'), ('json-formatter', 'JSON Formatter'), ('csv-to-json', 'CSV to JSON'), ('html-formatter', 'HTML Formatter')],
 'body': [
 ('p', 'SQL queries start simple and grow complicated. A quick <code>SELECT</code> becomes a query with several joins, a nested subquery, a handful of conditions and a grouping clause, and written as one long line it becomes almost impossible to read. Formatting SQL means laying it out with consistent indentation and line breaks so its logic is visible. The <a href="../tools/sql-formatter.html">SQL Formatter</a> does this in your browser, and this article explains why readable SQL matters and what a formatter standardises.'),
 ('p', 'Unlike some formatting, SQL formatting is not cosmetic. A well-laid-out query is genuinely easier to understand, review and debug, and the structure often reveals logical mistakes that a cramped one-liner hides.'),
 ('h2', 'Why long queries become unreadable'),
 ('p', 'SQL is declarative and clause-based: a query is built from clauses like <code>SELECT</code>, <code>FROM</code>, <code>JOIN</code>, <code>WHERE</code>, <code>GROUP BY</code> and <code>ORDER BY</code>, each doing a distinct job. When these run together on one line, or wrap awkwardly, you cannot tell at a glance where one clause ends and the next begins, which columns are being selected, or which conditions apply. The logic is all there, but it is hidden. Formatting puts each major clause on its own line and indents the details beneath it, so the shape of the query matches its meaning.'),
 ('h2', 'What a SQL formatter standardises'),
 ('p', 'A formatter applies a consistent style to several things at once:'),
 ('ul', ['<strong>Clause layout</strong>: major keywords start new lines, so the query reads top to bottom as a sequence of steps.', '<strong>Indentation</strong>: joined tables, conditions and grouped columns are indented to show they belong to a clause.', '<strong>Keyword casing</strong>: keywords are consistently upper- or lower-cased, separating SQL syntax from your table and column names.', '<strong>Comma placement and spacing</strong>: a uniform style for lists of columns and conditions.']),
 ('p', 'The result is that any query, however it was originally typed, comes out in one predictable house style that everyone on a team can read the same way.'),
 ('callout', 'Formatting can reveal logic bugs', 'When a query is properly indented, a condition attached to the wrong clause, a join without its expected condition, or a misplaced parenthesis in a subquery becomes visible. Cramped SQL hides these; formatted SQL surfaces them.'),
 ('h2', 'Reading complex queries at a glance'),
 ('p', 'The real payoff comes with genuinely complex queries. A formatted query with three joins and a subquery lets you follow the logic step by step: here are the columns we want, here are the tables and how they connect, here are the filters, here is how the results are grouped and ordered. You can review each clause in isolation. When debugging, this makes it far easier to isolate which part of the query is producing an unexpected result, because each part is visually distinct rather than buried in a run-on line.'),
 ('h2', 'When to format your SQL'),
 ('p', 'Reformatting a query is worth it whenever a query will be read by a human, which is almost always:'),
 ('ul', ['Before committing a query to a codebase or a migration, so reviewers can read it.', 'When debugging a slow or wrong query and you need to understand its structure.', 'When inheriting SQL written by someone else in an inconsistent style.', 'When copying a query out of application code, where it may have been concatenated into an unreadable string.']),
 ('tool', 'sql-formatter', 'SQL Formatter', 'Lay out messy SQL with consistent indentation and keyword casing for readability, entirely in your browser. Your query is never uploaded.'),
 ('h2', 'SQL among the other data formats'),
 ('p', 'SQL often sits alongside other structured data in a developer\'s day. Query results are frequently exported as JSON or CSV, and a <a href="../tools/json-formatter.html">JSON formatter</a> or <a href="../tools/csv-to-json.html">CSV to JSON</a> converter helps you inspect that output, just as the SQL formatter helps you read the query that produced it. The same readability principle runs through all of them: structured data and queries are far easier to work with when their layout reflects their structure.'),
 ('p', 'Formatting SQL locally also keeps queries private. A query can reveal your schema, your table and column names, and the logic of your business, details you may not want to paste into an unknown website. A browser-based formatter processes the query on your device, so nothing about your database design leaves your machine.'),
 ('h2', 'Formatting SQL you find in application code'),
 ('p', 'Some of the messiest SQL you will ever meet is the kind buried inside application code, where a query has been built by concatenating strings across several lines, sprinkled with quotes and plus signs and variable interpolations. Pulled out of that context, it is often a single unbroken line with no indentation at all, technically valid, humanly unreadable. Running it through a formatter is frequently the only practical way to understand what a query embedded in code actually does before you try to change it.'),
 ('p', "This comes up constantly during debugging and code review. A slow query flagged by the database, a report returning wrong numbers, an inherited feature nobody fully understands, in each case the first step is to extract the raw SQL and format it so its logic is legible. Once laid out, you can reason about the joins, spot a missing condition, or see that a filter is attached to the wrong clause. It is worth stressing again that doing this locally keeps your schema private: the query names your tables and columns and encodes your business logic, none of which belongs on a stranger's server. A browser-based formatter lets you make even sensitive, proprietary queries readable without exposing the design of your database to anyone."),
 ('p', 'Keep a formatter within reach for the next time you inherit a tangled query or need to understand your own from six months ago. Laying it out cleanly is usually the fastest first step toward understanding what it does, and doing it locally means your schema and business logic never leave your machine in the process.'),
 ('p', "In short, SQL's meaning is clause-based, and formatting makes those clauses visible, turning an impenetrable one-liner into a query you can read top to bottom. Format any query a person will read, let the layout expose logic mistakes, and keep a consistent style across your team. Readable SQL is easier to review, faster to debug, and less likely to hide the small errors that produce wrong results."),
 ],
 },
 {
 'slug': 'markdown-explained-for-beginners',
 'icon': '📝',
 'nav_title': 'Markdown explained',
 'headline': 'Markdown Explained: Write Formatted Text Without a Word Processor',
 'title': 'Markdown Explained: The Simple Formatting Syntax | 123MiniApps',
 'standfirst': 'What Markdown is, the handful of symbols that do everything, and why it has become the default for developers and writers.',
 'description': 'What Markdown is and how to write it. The core syntax for headings, links, lists and emphasis, why developers love it, and a free live Markdown preview tool.',
 'published': '2026-07-26',
 'keywords': ['markdown preview', 'what is markdown', 'markdown syntax', 'markdown to html', 'how to write markdown', 'markdown editor online'],
 'related_tools': [('markdown-preview', 'Markdown Preview'), ('html-formatter', 'HTML Formatter'), ('readability-checker', 'Readability Checker'), ('word-counter', 'Word Counter')],
 'body': [
 ('p', 'Markdown is a lightweight way to write formatted text using plain characters, so you can create headings, bold text, links and lists without a word processor or any HTML. You write <code>**bold**</code> and get bold; you write <code># Title</code> and get a heading. It was designed to be readable as-is, even before it is converted, which is why it has become the default format for developer documentation, README files, notes apps and many writing tools. The <a href="../tools/markdown-preview.html">Markdown Preview</a> shows the formatted result live as you type, in your browser.'),
 ('p', 'This article covers what Markdown is, the small set of symbols that does almost everything, and why so many people prefer writing in it over a traditional editor.'),
 ('h2', 'The idea behind Markdown'),
 ('p', "Markdown's founding principle is that the source should be readable even without being rendered. Where HTML wraps everything in tags, <code>&lt;strong&gt;bold&lt;/strong&gt;</code>: Markdown uses light punctuation that mimics how people already emphasise text in plain writing, like putting asterisks around a word. This means a Markdown document is perfectly readable as plain text, and becomes formatted HTML when passed through a converter. You get the benefits of formatting without the visual clutter of markup while you write."),
 ('h2', 'The core syntax'),
 ('p', 'A handful of symbols covers the vast majority of everyday formatting:'),
 ('ul', ['<strong>Headings:</strong> one to six <code>#</code> characters at the start of a line set the heading level.', '<strong>Emphasis:</strong> <code>*italic*</code> or <code>_italic_</code>, and <code>**bold**</code> for strong emphasis.', '<strong>Links:</strong> <code>[text](https://example.com)</code>.', '<strong>Lists:</strong> <code>-</code> or <code>*</code> for bullets, and <code>1.</code> for numbered lists.', '<strong>Code:</strong> backticks for `inline code`, and triple backticks for a code block.', '<strong>Quotes:</strong> a <code>&gt;</code> at the start of a line.']),
 ('p', 'That is enough to write almost any document. Everything else, images, tables, horizontal rules, follows the same spirit of simple, memorable punctuation.'),
 ('callout', 'Readable before and after', 'The test of good Markdown is that it reads cleanly as plain text. If your source is cluttered and hard to follow, you are probably overusing formatting. The syntax is meant to stay out of the way of the words.'),
 ('h2', 'Why developers and writers prefer it'),
 ('p', "Markdown's popularity comes from a combination of practical advantages. Because it is plain text, it works in any editor, is easy to track in version control (you can see exactly what changed line by line), and will never be locked in a proprietary format. Because the syntax is minimal, you can format as you type without lifting your hands from the keyboard to click toolbar buttons. And because it converts cleanly to HTML, the same document can become a web page, a PDF or documentation without being rewritten. For anyone who writes a lot of structured text, developers especially, this frictionless, portable, future-proof quality is compelling."),
 ('h2', 'Markdown to HTML and back'),
 ('p', 'Markdown is almost always converted to HTML for display, and a live preview is the best way to write it, because you see the rendered result beside your source and catch formatting mistakes instantly. The conversion is well-defined, so the same Markdown produces the same HTML everywhere, though some tools support extra features like tables and task lists on top of the core syntax. If you need to see or clean up the generated HTML, an <a href="../tools/html-formatter.html">HTML formatter</a> will lay it out readably.'),
 ('tool', 'markdown-preview', 'Markdown Preview', 'Write Markdown and see the formatted HTML render live as you type, entirely in your browser. Your text is never uploaded.'),
 ('h2', 'Getting started'),
 ('p', 'The best way to learn Markdown is to write it with a live preview open, so each symbol\'s effect is immediately visible. Start with headings and emphasis, add links and lists, and within a few minutes the syntax becomes second nature. Because you are writing plain text, you can also lean on writing tools alongside it, check length with a <a href="../tools/word-counter.html">word counter</a> and clarity with a <a href="../tools/readability-checker.html">readability checker</a>, since Markdown never gets in the way of the underlying words.'),
 ('h2', 'Where you will encounter Markdown'),
 ('p', 'Part of what makes learning Markdown worthwhile is how widely it is used, the same small syntax pays off across dozens of tools. It is the native format of README files and documentation on code-hosting platforms, where it renders automatically into formatted pages. It powers the comment and posting boxes of many developer communities and forums. A growing number of note-taking and writing apps use it as their underlying format, and static-site generators turn Markdown files directly into web pages. Learn it once and the skill transfers everywhere.'),
 ('p', "This ubiquity is not an accident; it flows from Markdown's plain-text nature. Because a Markdown document is just text, it works in any editor, survives being copied between apps, diffs cleanly in version control, and will still be readable decades from now regardless of what software exists then. Proprietary document formats offer none of these guarantees. For anyone who writes structured text regularly, documentation, notes, posts, articles, investing a few minutes in the core syntax returns a fast, portable, tool-agnostic way of writing that you will use for the rest of your career. Writing with a live preview at first makes the learning almost instant, and before long you will format text in Markdown without thinking about it, even in places that do not yet render it."),
 ('p', 'The best next step is simply to start writing in it. Open a live preview, jot a few notes using headings, bold, links and lists, and watch the formatting appear as you type. Within one short session the core symbols will feel natural, and you will have picked up a writing skill that works in more places than almost any other.'),
 ('p', 'Once the syntax is second nature you will reach for it everywhere, from quick notes to full documentation, confident that your plain-text source will remain readable and portable no matter which tool opens it next. Few skills this small repay the effort so consistently across so many contexts.'),
 ('p', 'In short, Markdown lets you write formatted text using simple, readable punctuation instead of tags or toolbars. Learn the handful of core symbols, write with a live preview until they are automatic, and you gain a fast, portable, version-control-friendly way to produce everything from quick notes to full documentation, all in plain text that stays readable whether or not it is ever rendered.'),
 ],
 },
 {
 'slug': 'http-status-codes-explained',
 'icon': '🚦',
 'nav_title': 'HTTP status codes',
 'headline': 'HTTP Status Codes Explained: What 404, 301 and 500 Really Mean',
 'title': 'HTTP Status Codes Explained: 404, 301, 500 & More | 123MiniApps',
 'standfirst': 'How the five classes of status code work, the ones you will actually meet, and what each tells you about a request.',
 'description': 'HTTP status codes explained: what the 2xx, 3xx, 4xx and 5xx classes mean, and the specific codes like 301, 404 and 500 you meet most, with a quick reference tool.',
 'published': '2026-07-25',
 'keywords': ['http status codes', 'what does 404 mean', '301 vs 302 redirect', '500 error meaning', 'http response codes list', 'status code reference'],
 'related_tools': [('http-status-codes', 'HTTP Status Codes'), ('jwt-decoder', 'JWT Decoder'), ('url-encoder-decoder', 'URL Encoder / Decoder'), ('regex-tester', 'Regex Tester')],
 'body': [
 ('p', 'Every time your browser requests a web page or an app calls an API, the server replies with a three-digit HTTP status code that says what happened. <code>200</code> means success, <code>404</code> means not found, <code>500</code> means the server broke. These codes are the shared vocabulary of the web, and understanding them turns a cryptic error into a clear diagnosis. The <a href="../tools/http-status-codes.html">HTTP Status Codes</a> reference lists them all, and this article explains how they are organised and which ones you will actually meet.'),
 ('p', 'The codes are not random numbers, they are grouped into five ranges by their first digit, and knowing what each range means lets you understand any code even if you have never seen it before.'),
 ('h2', 'The five classes of status code'),
 ('p', 'The first digit tells you the category of response:'),
 ('ul', ['<strong>1xx Informational</strong>: the request was received and the process is continuing. Rarely seen directly.', '<strong>2xx Success</strong>: the request succeeded. <code>200 OK</code> is the everyday case.', '<strong>3xx Redirection</strong>: further action is needed, usually following a redirect to another URL.', "<strong>4xx Client error</strong>: the request was faulty: wrong URL, missing permission, bad input. The problem is on the requester's side.", "<strong>5xx Server error</strong>: the request was fine but the server failed to fulfil it. The problem is on the server's side."]),
 ('p', 'That client-versus-server split in the 4xx and 5xx ranges is the most useful distinction of all: it immediately tells you which side to investigate.'),
 ('h2', 'The codes you will actually meet'),
 ('p', 'A handful of specific codes account for most of what you will encounter:'),
 ('ul', ['<strong>200 OK</strong>: success; the normal response for a working page or API call.', '<strong>301 Moved Permanently</strong>: this URL has permanently moved; update your links and let search engines pass authority to the new one.', '<strong>302 Found</strong>: a temporary redirect; the original URL still applies in the long run.', '<strong>404 Not Found</strong>: the requested resource does not exist at that URL.', '<strong>403 Forbidden</strong>: the resource exists but you are not allowed to access it.', '<strong>500 Internal Server Error</strong>: a generic server-side failure.', '<strong>503 Service Unavailable</strong>: the server is temporarily overloaded or down for maintenance.']),
 ('callout', '301 versus 302 matters for SEO', 'A 301 permanent redirect tells search engines to transfer ranking to the new URL; a 302 temporary redirect does not. Using the wrong one during a site move can cost you search visibility. When a move is permanent, use 301.'),
 ('h2', 'Reading codes to diagnose problems'),
 ('p', 'The class of a status code points straight at where to look. A 4xx means the request was wrong, so check the URL, the parameters, the authentication, or the data you sent. A 5xx means the server failed, so the fix is on the server side, check its logs, its resources, its dependencies. A 3xx means a redirect is involved, which matters when a request is not ending up where you expect. This triage, client error, server error, or redirect, is often the fastest first step in debugging any web request.'),
 ('h2', 'Status codes in APIs'),
 ('p', 'Status codes are just as central to APIs as to web pages. A well-designed API uses them precisely: 200 for a successful read, 201 for a created resource, 400 for invalid input, 401 for missing authentication, 403 for insufficient permission, 404 for a missing resource, 429 when you have made too many requests. Reading these correctly is essential when integrating with any service, because they tell you not just that something failed but why, and therefore how to fix your request.'),
 ('tool', 'http-status-codes', 'HTTP Status Codes', 'A searchable reference to every HTTP status code and what it means, right in your browser, handy whenever a response leaves you guessing.'),
 ('h2', 'A shared language worth knowing'),
 ('p', 'Because status codes are standardised, they mean the same thing across every server, framework and language, which is what makes them so valuable to learn once. When you are debugging alongside other tools, decoding a <a href="../tools/jwt-decoder.html">JWT</a> to check authentication behind a 401, or inspecting an encoded <a href="../tools/url-encoder-decoder.html">URL</a> behind a 404, the status code is the thread that ties the investigation together.'),
 ('h2', 'Status codes, caching and the browser'),
 ('p', 'Beyond signalling success and failure, status codes drive real browser behaviour, which is why using the right one matters. A 301 permanent redirect is cached aggressively by browsers, once a browser learns that a URL has permanently moved, it may stop requesting the old one entirely and go straight to the new location, sometimes for a very long time. This makes 301 powerful but unforgiving: set one up by mistake and visitors can keep being redirected long after you have removed the rule. A 302 temporary redirect, by contrast, is not cached the same way, which is exactly why it exists for genuinely temporary moves.'),
 ('p', 'Other codes interact with caching and retries too. A 304 Not Modified tells the browser its cached copy is still current, saving a full download, a quiet workhorse behind fast repeat visits. A 429 Too Many Requests signals that a client should slow down and try again later, often with a hint about how long to wait, and well-behaved clients respect it rather than hammering the server. Understanding these behaviours turns status codes from passive labels into active tools: choosing the correct code shapes how browsers, proxies and API clients treat your responses. Getting them right is part of building a site or API that is fast, correct and considerate of the systems talking to it.'),
 ('p', 'Keep a reference handy for the codes you meet less often, and internalise the five classes so you can interpret any response at a glance. Whether you are debugging a broken link, a failing API call or a redirect that loops, the status code is almost always your first and most reliable clue about what went wrong and where to look.'),
 ('p', "To sum up: HTTP status codes are the web's standard way of reporting what happened to a request, organised into five classes by their first digit. Learn the classes and you can interpret any code; learn the dozen or so common ones and you can diagnose most everyday problems at a glance. The difference between 301 and 302, or between a 4xx and a 5xx, is often the single most useful clue you have when something on the web is not behaving."),
 ],
 },
]
